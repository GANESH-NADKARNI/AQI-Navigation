fastapi==0.111.0
uvicorn[standard]==0.30.1
httpx==0.27.0
python-dotenv==1.0.1
joblib==1.4.2
scikit-learn==1.5.0
xgboost==2.0.3
lightgbm==4.3.0
numpy==1.26.4
pandas==2.2.2
pydantic==2.7.1

# Deep Learning (for app_dl.py / ml/train_dl.py)
# CPU-only install (smaller, no CUDA required):
# pip install torch --index-url https://download.pytorch.org/whl/cpu
#
# GPU install (if you have NVIDIA CUDA 12):
# pip install torch --index-url https://download.pytorch.org/whl/cu121
#
# torch is NOT listed here because the index-url must be passed manually.
# Run the appropriate pip command above after installing the other deps.
